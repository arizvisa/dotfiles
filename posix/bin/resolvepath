#!/bin/sh
path=$1
shift

if test -z "$path"; then
    path=$(pwd)
fi

path=$(readlink -f "$path")

if test ! -d "$path"; then
    return 1
fi

if test ! "${CYGWIN}"; then
  echo "$path"
else
  echo "$path" | /usr/bin/cygpath -u
fi
