#!/bin/sh
path=$1
shift

[ -z "$path" ] && path=`pwd`
path=`readlink -f "$path"`

[ ! -d "$path" ] && exit 1

case "$platform" in
    Cygwin|Msys) echo "$path" | xargs cygpath -u -- ;;
    *) echo "$path" ;;
esac

if [ "$#" -gt 0 ]; then
    $0 "$@" || exit 1
fi
